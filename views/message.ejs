<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title>Message Details</title>
  </head>
  <body>
    <div class="main-container">
      <h1>Message Details</h1>
      <div class="message">
        <h2>From: <%= message.username %></h2>
        <p><%= message.message %></p>
        <p>
          <em>
            PHILIPPINES: <%= new Intl.DateTimeFormat('en-PH', { dateStyle:
            'medium', timeStyle: 'short', timeZone: 'Asia/Manila' }).format(new
            Date(message.posted_at)) %>
            <br />
          </em>
        </p>
      </div>
      <a class="back-link" href="/">&#x25c0; Back to messages</a>
    </div>
  </body>
</html>
